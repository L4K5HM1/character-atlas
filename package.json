{
  "name": "character-atlas",
  "version": "2.0.0",
  "private": true,
  "description": "Form-aware character comparisons with transparent editorial rankings.",
  "scripts": {"test": "node --test tests/*.test.cjs"},
  "engines": {"node": ">=20"}
}
